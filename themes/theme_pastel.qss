#bottomWidget {
    /* Очень плотный, почти непрозрачный белый с легчайшим лавандово-розовым подтоном */
    background-color: rgba(252, 248, 255, 255); 
    /* Тонкая лавандовая линия сверху, чтобы отделить панель от основного окна */
    border-top: 1px solid rgba(200, 198, 250, 200);
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
}

#leftWidget,
#rightWidget {
    background: transparent;
}

#topSplitter::handle {
    background-color: transparent;
}

/* Suppress default Qt styling artifacts in tree/list views */
#fileTreeView,
#fileListView,
#searchResultsView {
    outline: none;
    border: none;
    show-decoration-selected: 0;
}

#fileTreeView::item:focus,
#fileListView::item:focus,
#searchResultsView::item:focus {
    outline: none;
    border: none;
    background-color: transparent;
}

#fileTreeView::item:hover,
#fileListView::item:hover,
#searchResultsView::item:hover {
    background-color: transparent;
}

#fileTreeView::item:selected,
#fileListView::item:selected,
#searchResultsView::item:selected {
    background-color: transparent;
}

#leftWidget QPushButton,
#rightWidget QPushButton {
    border: none;
    border-radius: 8px;
    background: transparent;
}

#leftWidget QPushButton:hover,
#rightWidget QPushButton:hover {
    background-color: rgba(220, 208, 255, 150);
}

#leftWidget QPushButton:pressed,
#rightWidget QPushButton:pressed {
    background-color: rgba(200, 198, 250, 190);
}

#searchBox {
    background: rgba(255, 255, 255, 150);
    color: #5D5466;
    border: 1px solid rgba(200, 198, 250, 180);
    border-radius: 14px;
    padding: 0 12px;
    margin-left: 10px;
}

#searchBox:focus {
    border: 2px solid #FFA8B6;
    background: rgba(255, 255, 255, 180);
}

#timeLabel,
#metadataLabel {
    color: #3B3542;
    font-weight: 600;
}

#metadataLabel {
    background-color: rgba(255, 255, 255, 240);
    border: 1px solid rgba(200, 198, 250, 200);
    border-top: none;
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
}

#visualizerWidget {
    /* Глубокий угольно-ежевичный цвет для контраста радуги */
    background-color: rgba(59, 53, 66, 240); 
    border: 1px solid rgba(200, 198, 250, 200);
    border-bottom: none;
    border-top-left-radius: 12px;
    border-top-right-radius: 12px;
}

#trackSeparator {
    background-color: rgba(59, 53, 66, 100);
    min-height: 5px;
    max-height: 5px;
    border-left: 1px solid rgba(200, 198, 250, 200);
    border-right: 1px solid rgba(200, 198, 250, 200);
    border-top: none;
    border-bottom: none;
}

#separatorLine {
    background-color: rgba(59, 53, 66, 100);
    min-height: 5px;
    max-height: 5px;
    border: none;
}

#volumeButton {
    min-width: 25px;
    min-height: 25px;
    background-color: transparent;
    border: none;
    qproperty-flat: true;
}

#treeButton:hover,
#listButton:hover,
#volumeButton:hover {
    background-color: rgba(200, 198, 250, 180);
}

#prevButton,
#nextButton {
    background-color: #c8c6fa;
    border: 1px solid transparent;
    border-radius: 8px;
    padding: 6px;
    min-width: 14px; /* let padding and icon size dictate */
    min-height: 14px;
}

#prevButton:hover,
#nextButton:hover {
    background-color: #a6a3f0;
}

#playPauseButton {
    background-color: #ffa8b6;
    border: 1px solid transparent;
    border-radius: 8px;
    padding: 6px;
    min-width: 14px;
    min-height: 14px;
}

#playPauseButton:hover {
    background-color: #ff8a9d;
}

#bufferProgressBar {
    background: rgba(0, 0, 0, 0.05); /* Легкая тень вместо белого */
    border: none;
    border-radius: 3px;
    margin-top: 9px;
    margin-bottom: 9px;
}

#bufferProgressBar::chunk {
    background: rgba(200, 198, 250, 0.7); /* Мягкий лавандовый буфер */
    border-radius: 3px;
}

#timelineSlider::groove:horizontal,
#volumeSlider::groove:horizontal {
    background: transparent;
    height: 6px;
    border-radius: 3px;
}

/* Закрашенная часть ползунка (теперь розовая, а не синяя) */
#timelineSlider::sub-page:horizontal,
#volumeSlider::sub-page:horizontal {
    background: #ffa8b6; 
    height: 6px;
    border-radius: 3px;
}

/* Пустая часть ползунка */
#timelineSlider::add-page:horizontal,
#volumeSlider::add-page:horizontal {
    background: transparent;
    height: 6px;
    border-radius: 3px;
}

/* Сам кружок (белый с лавандовой обводкой) */
#timelineSlider::handle:horizontal,
#volumeSlider::handle:horizontal {
    background: #FFFFFF;
    border: 1px solid rgba(200, 198, 250, 200);
    width: 16px;
    height: 16px;
    margin: -5px 0;
    border-radius: 8px;
}

/* Кружок при наведении (яркий розовый, вместо старого агрессивного красного) */
#timelineSlider::handle:horizontal:hover,
#volumeSlider::handle:horizontal:hover {
    background: #ff8a9d;
    border: 1px solid #ff8a9d;
}

#fileTreeView,
#fileListView,
#searchResultsView {
    background: transparent;
    border: none;
    outline: none;
    color: #3B3542;
    selection-background-color: transparent;
    selection-color: #3B3542;
}

#rightWidget QScrollBar:vertical {
    background: rgba(0, 0, 0, 0.03);
    border: none;
    width: 14px;
    margin: 0px 6px 12px 0px; /* Top=0 to start higher, Right=6px */
}

#rightWidget QScrollBar:horizontal {
    background: rgba(0, 0, 0, 0.03);
    border: none;
    height: 14px;
    margin: 0px 12px 6px 12px;
}

#rightWidget QScrollBar::handle:vertical,
#rightWidget QScrollBar::handle:horizontal {
    background: #DCD0FF;
    border-radius: 7px;
    min-height: 24px;
    min-width: 24px;
}

#rightWidget QScrollBar::handle:vertical:hover,
#rightWidget QScrollBar::handle:horizontal:hover {
    background: #C8C6FA;
}

#rightWidget QScrollBar::add-line:vertical,
#rightWidget QScrollBar::sub-line:vertical,
#rightWidget QScrollBar::add-line:horizontal,
#rightWidget QScrollBar::sub-line:horizontal {
    background: none;
    border: none;
    width: 0px;
    height: 0px;
}

#fileTreeView::item,
#fileListView::item,
#searchResultsView::item {
    padding: 4px 0px;
    border: none;
    outline: none;
}

#fileTreeView::item:selected,
#fileTreeView::item:hover,
#fileListView::item:selected,
#fileListView::item:hover,
#searchResultsView::item:selected,
#searchResultsView::item:hover {
    background: transparent;
    color: transparent;
}

#treeButton,
#listButton,
#queueButton,
#darkThemeButton,
#pastelThemeButton {
    background: transparent;
    border: 2px solid transparent;
    border-radius: 6px;
    padding: 4px 8px;
    color: #3B3542;
    font-weight: 400;
    min-width: 35px;
    min-height: 22px;
}

#darkThemeButton:checked {
    background-color: rgba(220, 208, 255, 150);
    border: 2px solid #FFA8B6;
}

#pastelThemeButton:checked {
    background-color: rgba(220, 208, 255, 150);
    border: 2px solid #FFA8B6;
}

#darkThemeButton:hover,
#pastelThemeButton:hover {
    background-color: rgba(200, 198, 250, 180);
    border: 2px solid rgba(220, 208, 255, 150);
}

#darkThemeButton:pressed,
#pastelThemeButton:pressed {
    background-color: rgba(200, 198, 250, 220);
}

#interactiveBackground {
    background-color: rgb(180, 215, 255);
}

/* --- Context Menu (Pastel Soft) --- */
QMenu {
    background-color: rgba(255, 255, 255, 245);
    border: 1px solid rgba(200, 198, 250, 200);
    border-radius: 8px;
    padding: 4px;
    color: #3B3542;
}

QMenu::item {
    padding: 6px 24px 6px 12px;
    border-radius: 4px;
    background-color: transparent;
}

QMenu::item:selected {
    background-color: rgba(220, 208, 255, 150);
    color: #3B3542;
}

QMenu::separator {
    height: 1px;
    background: rgba(200, 198, 250, 150);
    margin: 4px 8px;
}

/* --- Theme Switcher (Pastel Dream) --- */
#themeSwitchContainer {
    background-color: rgba(255, 255, 255, 120);
    border: 1px solid rgba(200, 198, 250, 150);
    border-radius: 14px;
}

#themeSeparator {
    background-color: rgba(200, 198, 250, 150);
    min-width: 1px;
    max-width: 1px;
    margin: 6px 0px;
}

#darkThemeButton, #pastelThemeButton {
    background: transparent;
    color: #5D5466;
    padding: 6px 14px;
    font-weight: 500;
    font-size: 16px;
    border: none;
}

#darkThemeButton {
    border-top-left-radius: 13px;
    border-bottom-left-radius: 13px;
}

#pastelThemeButton {
    border-top-right-radius: 13px;
    border-bottom-right-radius: 13px;
}

#darkThemeButton:hover, #pastelThemeButton:hover {
    background-color: rgba(200, 198, 250, 80);
}

#darkThemeButton:checked, #pastelThemeButton:checked {
    background-color: #FFA8B6;
    color: #3B3542;
    font-weight: 600;
}

/* --- Custom Title Bar (Pastel) --- */
#customTitleBar {
    background-color: rgb(31, 41, 55);
}
#titleBarLabel {
    color: #E9EDF5; /* Keep text readable on dark background */
    font-weight: 600;
    font-size: 14px;
}
#titleBarBtn, #titleBarCloseBtn {
    background: transparent;
    border: none;
    border-radius: 15px;
    color: #E9EDF5;
    font-size: 14px;
    font-family: sans-serif;
}
#titleBarBtn:hover {
    background-color: rgba(220, 208, 255, 150);
}
#titleBarBtn:pressed {
    background-color: rgba(200, 198, 250, 190);
}
#titleBarCloseBtn:hover {
    background-color: #ff8a9d;
    color: white;
}
#titleBarCloseBtn:pressed {
    background-color: #ffa8b6;
}